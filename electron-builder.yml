appId: com.ednovas.cloud
productName: "EdNovas云"
compression: maximum
# 🟢 仅保留中英文语言包，移除其他几十种语言
electronLanguages:
  - zh-CN
  - en-US
directories:
  output: dist_electron
files:
  - "dist/**/*"
  - "dist-electron/**/*"
  - "package.json"
  - "!**/*.map"
  - "!node_modules/**/*" # 🟢 彻底排除 node_modules (因为 main.js 已经打包，且前端依赖已在 dist 中)
# 关键配置：将 resources 目录打包到软件中
extraResources:
  - from: "resources/"
    to: "resources/"
win:
  target: nsis
  icon: "public/icon.png" # 确保你有一个图标文件
  requestedExecutionLevel: requireAdministrator #如果需要TUN模式，最好请求管理员权限
  artifactName: "EdNovas-Cloud-${version}-Windows-x64.${ext}"

mac:
  target: dmg
  icon: "public/icon.png"
  artifactName: "EdNovas-Cloud-${version}-macOS-${arch}.${ext}"

linux:
  target:
    - AppImage
    - deb
  icon: "public/icon.png"
  category: Network
  maintainer: "EdNovas <ednovas@ednovas.tech>"
  synopsis: "EdNovas Cloud Proxy Client"
  description: "Official desktop client for EdNovas Cloud services."
  artifactName: "EdNovas-Cloud-${version}-Linux-${arch}.${ext}"
  deb:
    priority: optional
    compression: xz
  desktop:
    StartupWMClass: ednovas-cloud
    Icon: ednovas-cloud
    Categories: Network;

nsis:
  oneClick: false # 🟢 关闭一键安装，允许用户点击下一步
  allowToChangeInstallationDirectory: true # 🟢 允许用户修改安装目录
  perMachine: true # 🟢 建议默认安装到 Program Files (全机可用)，设为 false 则默认 AppData (仅当前用户)
  createDesktopShortcut: true # 🟢 创建桌面快捷方式
  createStartMenuShortcut: true # 🟢 创建开始菜单快捷方式
  shortcutName: "EdNovas云" # 快捷方式名称
